{
  "name": "@tryabby/worker",
  "version": "0.0.1",
  "scripts": {
    "dev": "dotenv -e ../../.env -c -v WATCH=1 tsup",
    "start": "node dist/index.js",
    "start:docker": "turbo run db:migrate-prod && node dist/index.js",
    "build": "rm -rf dist && tsup",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bull-board/api": "^5.13.0",
    "@bull-board/express": "^5.13.0",
    "@tryabby/core": "workspace:*",
    "@tryabby/db": "workspace:*",
    "@tryabby/queue": "workspace:*",
    "@tryabby/redis": "workspace:*",
    "@tryabby/services": "workspace:*",
    "bullmq": "^5.1.1",
    "express": "^4.18.2",
    "express-basic-auth": "^1.2.1",
    "pino": "^8.17.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "eslint": "^8.48.0",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2"
  },
  "eslintConfig": {
    "root": true,
    "extends": [
      "eslint-config-custom"
    ]
  }
}
